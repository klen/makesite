[Main]
db_name={{ project.replace('-', '_') }}_master
db_user={{ project.replace('-', '_') }}
db_password={{ project.replace('-', '_') }}

; For auto create databases and users
mysql_user=root
mysql_password=root
mysql_host=localhost
mysql_port=3306
